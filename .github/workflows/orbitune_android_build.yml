name: Android Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test:
    name: Test and Analyze
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install dependencies
        run: flutter pub get
      - name: Analyze project source
        run: flutter analyze
      - name: Run tests
        run: flutter test

  build-debug:
    name: Build Debug APK
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install dependencies
        run: flutter pub get
      - name: Build Debug APK
        run: flutter build apk --debug --split-per-abi
      - name: Rename Debug APKs
        run: |
          cd build/app/outputs/flutter-apk/
          for file in app-*-debug.apk; do
            arch=$(echo $file | sed 's/app-\(.*\)-debug\.apk/\1/')
            mv "$file" "orbitune_${arch}_debug.apk"
          done
      - name: Upload arm64-v8a Debug
        uses: actions/upload-artifact@v4
        with:
          name: orbitune_arm64-v8a_debug
          path: build/app/outputs/flutter-apk/orbitune_arm64-v8a_debug.apk
      - name: Upload armeabi-v7a Debug
        uses: actions/upload-artifact@v4
        with:
          name: orbitune_armeabi-v7a_debug
          path: build/app/outputs/flutter-apk/orbitune_armeabi-v7a_debug.apk

  build-release:
    name: Build Release APK
    needs: build-debug
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
      - name: Install dependencies
        run: flutter pub get
      - name: Decode Android keystore
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          $keystoreBytes = [System.Convert]::FromBase64String($env:KEYSTORE_BASE64)
          [System.IO.File]::WriteAllBytes("android/app/upload-keystore.jks", $keystoreBytes)
        shell: pwsh
      - name: Create key.properties
        run: |
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" > android/key.properties
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/key.properties
          echo "storeFile=upload-keystore.jks" >> android/key.properties
        shell: bash
      - name: Build Release APK
        run: flutter build apk --release --split-per-abi
      - name: Rename Release APKs
        run: |
          cd build/app/outputs/flutter-apk/
          for file in app-*-release.apk; do
            arch=$(echo $file | sed 's/app-\(.*\)-release\.apk/\1/')
            mv "$file" "orbitune_${arch}_release.apk"
          done
      - name: Upload arm64-v8a Release
        uses: actions/upload-artifact@v4
        with:
          name: orbitune_arm64-v8a_release
          path: build/app/outputs/flutter-apk/orbitune_arm64-v8a_release.apk
      - name: Upload armeabi-v7a Release
        uses: actions/upload-artifact@v4
        with:
          name: orbitune_armeabi-v7a_release
          path: build/app/outputs/flutter-apk/orbitune_armeabi-v7a_release.apk
